template header

name
object
conditional
actions
priority

package eubrazil.atmosphere.templates;

global eubr.atmosphere.tma.entity.qualitymodel.TrustworthinessObject trustworthinessObject;

import java.text.SimpleDateFormat;
import eubrazil.atmosphere.planning.AdaptationManager;
import function eubrazil.atmosphere.util.drools.DroolsUtility.debug;

dialect "java"

template "Trustworthiness"

rule "@{name}" 
enabled true
salience @{priority}
when
    @{object}(@{conditional})
then
    AdaptationManager.performAdaptation(@{actions});
	debug(drools);
end

end template